  import 'dart:ui';
  import 'package:flutter/material.dart';


  const Color lightBackground = Color(0xFFE8EAF6);
  const Color primaryDark = Color(0xFF6F91E6); // Midnight Blue
  const Color primaryLight = Color(0xFF3F51B5); // Indigo/Accent Blue
  const Color yellowColor = Color(0xFFFFC107); // Amber/Gold for highlights
  const Color primaryColor = Color(0xFF546180);
  const Color accentColor = Color(0xFF4B7DFF);
  const Color primaryPinkFaint = Color(0xFFF6C1DA);




  // =================== Primary ===================
  const Color primaryDarkBlue = Color(0xFF1A2A55); // deep blue, trust & professional
  const Color primaryLightBlue = Color(0xFF3F70FF); // vibrant accent
  const Color primaryTeal = Color(0xFF00BFA6); // health/fresh feel
  const Color primaryPink = Color(0xFFEC4899); // premium highlight

  // =================== Backgrounds ===================
  const Color lightBg = Color(0xFFF4F8FA); // main background, very light
  const Color cardBackground = Colors.white; // for cards
  const Color secondaryBg = Color(0xFFE0F7FA); // faint sky blue for headers or tags

  // =================== Text ===================
  const Color primaryText = Color(0xFF202A44); // main dark text
  const Color secondaryText = Color(0xFF5C6BC0); // secondary text
  const Color accentText = Color(0xFF4B7DFF); // for buttons/links

  // =================== Highlights / Actions ===================
  const Color accentTeal = Color(0xFF4AC601); // positive indicators (good health, progress)
  const Color warningYellow = Color(0xFFFFC107); // alerts or upcoming events
  const Color errorRed = Color(0xFFEF4444); // emergency / urgent

  // =================== Borders & Shadows ===================
  const Color borderColor = Color(0xFFCBD5E1);
  const Color tableHeaderBg = Color(0xFFE0E7FF);

  // =================== Faint gradients for premium feel ===================
  const Color gradientPinkFaint = Color(0xFFF6C1DA);
  const Color gradientSkyFaint = Color(0xFFE0F7FA);
